import React, {Component} from "react";

class Footer extends Component {
    render () {
        return (
            <footer className="page-footer">
                <h5 className="grey-text">No copyright exists for me</h5>
            </footer>
        );
    }
}

export default Footer;